import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder test suite — expand as features are tested.
  // media_kit requires a native platform to initialise so full widget
  // tests are run on-device; these unit-style checks are CI-safe.
  test('placeholder — always passes', () {
    expect(true, isTrue);
  });
}
